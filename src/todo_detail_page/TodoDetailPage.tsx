import React from "react";
import { useParams } from "react-router-dom";

const TodoDetailPage = () => {
  const { id } = useParams();

  return <div>TodoDet ailPage</div>;
};

export default TodoDetailPage;



 