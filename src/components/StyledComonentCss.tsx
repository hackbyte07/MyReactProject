import './StyledComponent.css'; 

const StyledComponent = () => {
  return (
    <div className="container">
      <h2 className="text">Styled Component</h2> 
      <p className="text">This component uses a separate CSS file.</p> 
    </div>
  );
};

export default StyledComponent;
